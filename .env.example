# Frontend Environment Variables
# Copy to .env.local and fill in real values. Never commit .env.local to version control.

# API URL - must include /api suffix
VITE_API_URL=https://api.yourdomain.com/api